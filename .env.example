# CollegeIQ Environment Variables Configuration

# PostgreSQL Connection String (e.g. Neon PostgreSQL, AWS RDS, Supabase, or local PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://user:password@ep-sample-project.neon.tech/collegeiq?sslmode=require"

# Application Base URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
